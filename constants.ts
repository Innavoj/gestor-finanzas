
export const API_BASE_URL = 'http://localhost:5001/api';

// initialProducts and initialTransactions are no longer used for runtime state,
// but can be kept for reference or testing if desired.
// export const initialProducts: Product[] = [ ... ];
// export const initialTransactions: Transaction[] = [ ... ];

// generateId is no longer needed as backend will generate IDs.
// export const generateId = (): string => Date.now().toString(36) + Math.random().toString(36).substring(2, 9);